<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leaf.myapp.dao.EmployeeDAO">
	<!--  <select id="checkJoinId" resultType="com.bitcamp.myapp.vo.EmployeeVO">
		select userid,username,tel,email from registers
		where userid=#{userid};
	</select>-->
</mapper>